apiVersion: v1
kind: Secret
metadata:
  name: vayva-secrets
  namespace: vayva
type: Opaque
stringData:
  # These are placeholders - use external secrets operator or sealed secrets in production
  database-url: "postgresql://user:pass@localhost:5432/vayva"
  redis-url: "redis://localhost:6379"
  jwt-secret: "change-me-in-production"
