apiVersion: v1
kind: ConfigMap
metadata:
  name: vayva-config
  namespace: vayva
data:
  NODE_ENV: "production"
  PORT: "3000"
  LOG_LEVEL: "info"
  API_VERSION: "v1"
  ENABLE_METRICS: "true"
  METRICS_PORT: "9090"
  HEALTH_CHECK_PATH: "/health/live"
  READINESS_CHECK_PATH: "/health/ready"
