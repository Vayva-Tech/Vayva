import { ExtensionManifest } from "./types";
/**
 * Fetches an external extension manifest from a URL and validates its structure.
 */
export declare function fetchAndValidateManifest(url: string): Promise<ExtensionManifest>;
//# sourceMappingURL=manifest-fetcher.d.ts.map