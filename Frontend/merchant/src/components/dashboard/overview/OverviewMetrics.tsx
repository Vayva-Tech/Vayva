"use client";

/**
 * Legacy overview metrics placeholder — replaced by universal dashboard KPIs.
 * Kept so barrel exports and older imports keep resolving.
 */
export function OverviewMetrics() {
  return null;
}
